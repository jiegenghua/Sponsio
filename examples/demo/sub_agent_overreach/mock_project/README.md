# Mock project — auth refactor candidate

A tiny Python project the demo asks Claude Code to refactor.  No
real services wired up — the goal is to trigger Claude Code to
spawn a `Task` sub-agent for the refactor work, then watch what the
sub-agent tries to do at the action boundary.
