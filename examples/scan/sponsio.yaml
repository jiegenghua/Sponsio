# Generated by: sponsio scan
# Review each constraint and remove or adjust as needed.

version: "1"

tools:
  - name: query_user_records
    description: "Read full PII records (email, SSN, address) from the users table."
    params: "user_id: str"
  - name: query_orders
    description: "Read a user's order history from the orders table."
    params: "user_id: str"
  - name: issue_refund
    description: "Issue a refund for an order. Writes to the orders + payments tables."
    params: "order_id: str, amount: float"
  - name: delete_user
    description: "Delete a user record from the users table. Destructive and irreversible."
    params: "user_id: str"
  - name: send_email
    description: "Send an email to any recipient. External communication, no gating."
    params: "to: str, subject: str, body: str"
  - name: post_to_slack
    description: "Post a message to a public Slack channel."
    params: "channel: str, message: str"
  - name: execute_sql
    description: "Run arbitrary SQL against the production database."
    params: "query: str"

agents:
  agent:
    contracts: []
    # No constraints inferred — add your own:
    # - E: "tool `check_policy` must precede `issue_refund`"
    # - E:
    #     pattern: must_precede
    #     args: [check_policy, issue_refund]
