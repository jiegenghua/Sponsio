{
  "name": "@sponsio/openclaw",
  "version": "0.1.0-alpha.2",
  "description": "Runtime contract guardrails for OpenClaw plugins — delegates each `before_tool_call` to the same `sponsio plugin guard --stdin` backend used by the sponsio-claude-code plugin, so per-plugin libraries under `~/.sponsio/plugins/` work with both runtimes.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md",
    "QUICKSTART.md"
  ],
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-strip-types --test test/integration.test.ts"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/SponsioLabs/Sponsio",
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "5.4"
  }
}
